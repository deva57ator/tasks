{
  "name": "tasks-backend",
  "version": "0.1.0",
  "description": "API backend for mini task tracker",
  "private": true,
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node scripts/migrate.js",
    "lint": "node scripts/lint.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.5",
    "dotenv": "^16.4.5",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "pino": "^9.1.0",
    "pino-http": "^10.3.0",
    "zod": "^3.23.8"
  }
}
